## Description:

In C, create a doubly linked list of heap allocated strings. Create functions to perform insertion, finding, and deletion.

## What I learned:

This challenge was a good re-introduction into compiling with (-g) and using GDB. 
Had to make break points at main.c:14 main.c:15 and main.c:18 due to a silly mistake 
of a missing return statement. Always a good-fun time when the mistake is so basic you
think it cannot possibly be true.